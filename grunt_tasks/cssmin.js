module.exports = {
    frontend_build: {
        options: {
            banner: '/*! <%= pkg.name %>: Minified css file <%= grunt.template.today("yyyy-mm-dd") %> */\n'
        },
        files: {
        }
    },
}
